# Database — PostgreSQL connection string
# If using docker-compose.dev.yml: postgresql://postgres:postgres@localhost:5432/iridium
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/iridium"

# VoltAgent Memory — separate PostgreSQL instance for AI agent memory
# If using docker-compose.dev.yml: postgresql://postgres:postgres@localhost:5433/voltagent
VOLTAGENT_DATABASE_URL="postgresql://postgres:postgres@localhost:5433/voltagent"

# Host ports docker-compose.dev.yml publishes the two databases on. `bun run
# setup` moves them off 5432/5433 when another project holds those; if you
# change them by hand, keep the two URLs above on the same ports.
POSTGRES_PORT="5432"
VOLTAGENT_POSTGRES_PORT="5433"

# Better Auth — session encryption secret (min 32 chars)
# Generate one: openssl rand -base64 32
BETTER_AUTH_SECRET=""

# Better Auth — base URL (must match where the app is running)
BETTER_AUTH_BASE_URL="http://localhost:5173"

# Optional: first-admin bootstrap. Comma-separated emails promoted to ADMIN
# once the address is verified (never at sign-up).
ADMIN_EMAILS=""

# Anthropic — powers AI chat. Without it the app still boots (in dev) but chat
# is disabled; set a real key to enable it.
ANTHROPIC_API_KEY=""

# Optional: Stripe billing. Without STRIPE_SECRET_KEY, billing runs in stub
# mode (mock checkout/portal sessions logged to the console) so dev needs no
# Stripe account. See app/lib/billing.server.ts to wire the real SDK.
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRICE_ID=""
